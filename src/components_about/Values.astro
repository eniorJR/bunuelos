---
const values = [
  {
    title: "Tradición",
    description: "Preservamos las recetas y técnicas ancestrales que han pasado de generación en generación en nuestra familia.",
    icon: "♥"
  },
  {
    title: "Calidad",
    description: "Seleccionamos cuidadosamente cada ingrediente para garantizar el sabor auténtico y la textura perfecta en cada buñuelo.",
    icon: "✓"
  },
  {
    title: "Artesanía",
    description: "Cada buñuelo es elaborado a mano con dedicación y atención al detalle por nuestros maestros pasteleros.",
    icon: "✦"
  },
  {
    title: "Innovación",
    description: "Respetamos la tradición pero exploramos nuevos sabores y técnicas para ofrecer experiencias únicas a nuestros clientes.",
    icon: "★"
  }
];
---

<section class="py-16 bg-amber-50">
  <div class="container mx-auto px-4">
    <div class="max-w-3xl mx-auto text-center mb-12">
      <h2 class="text-3xl font-bold text-amber-800 mb-4">Nuestros Valores</h2>
      <p class="text-gray-700">
        Los principios que guían nuestra pasión por crear los mejores buñuelos artesanales
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {values.map(value => (
        <div class="bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition-shadow">
          <div class="w-16 h-16 bg-amber-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-amber-600 text-2xl">
              {value.icon === "quality" && "✓"}
              {value.icon === "tradition" && "♥"}
              {value.icon === "craft" && "✦"}
              {value.icon === "innovation" && "★"}
            </span>
          </div>
          <h3 class="text-xl font-semibold text-amber-800 mb-3 text-center">
            {value.title}
          </h3>
          <p class="text-gray-600 text-center">{value.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>